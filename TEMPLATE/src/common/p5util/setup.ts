/**
 * ---- p5util setup ----------------------------------------------------------
 */

import p5 from "p5";

/**
 * A list of functions that will be called in `p.setup()`.
 */
export const onSetup: ((p: p5) => void)[] = [];
