/**
 * ---- Settings --------------------------------------------------------------
 */

/** The id of the HTML element to which the canvas should belong. */
export const HTML_ELEMENT_ID = "InfoPollution";

/** The logical height of the canvas. */
export const LOGICAL_CANVAS_HEIGHT = 800;

/** The font name. */
export const fontName = "Squada One";

/** The font size. */
export const fontSize = 48;

/** The line height. */
export const lineHeight = 1.5 * fontSize;

/** Interval between two letters. */
export const letterInterval = 5;
