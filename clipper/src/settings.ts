/**
 * ---- Settings --------------------------------------------------------------
 */

/** The id of the HTML element to which the canvas should belong. */
export const HTML_ELEMENT_ID = "Clipper";

/** The logical width of the canvas. */
export const LOGICAL_CANVAS_WIDTH = 1600;

/** The logical height of the canvas. */
export const LOGICAL_CANVAS_HEIGHT = 900;

/** The directory path of assets. */
export const assetPath = "assets/";
